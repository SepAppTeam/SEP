<tr id=<%= "categoria_#{categoria.id}" %>>
	<td><%= categoria.id %></td>
	<td><%= categoria.nombre %></td>
	<td><%= categoria.descripcion %></td>
	<td> 
	  <%= link_to  image_tag('Crud/mostrar.png'), categoria, :remote => true, :class => 'btn btn-mini' %>
      <%= link_to image_tag('Crud/editar1.png', :title => "Editar Equipos"), edit_categoria_path(categoria), :remote => true,
          :class => 'btn btn-mini'%>
	</td>
</tr>


